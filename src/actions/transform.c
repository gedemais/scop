#include "main.h"

void	rotate_mesh(t_env *env)
{
	(void)env;
}

void	transate_mesh(t_env *env)
{
	(void)env;
}

// Moves directions :

void	move_object(t_env *env, int key)
{
	(void)env;
	(void)key;
}
